# Índice do Catálogo de Dados

Documentação legível para o negócio de cada modelo dbt do projeto, organizada por camada.

{% if project.metrics %}
Consulte o [Glossário de Métricas](metrics.md) para as {{ project.metrics | length }} métrica(s) disponíveis na camada semântica.

{% endif %}
{% for layer, models in layers %}
## {{ layer or "(sem schema)" }}

| Modelo | Descrição | Documentação |
| --- | --- | --- |
{% for model in models %}
| {{ (model.label ~ " (`" ~ model.name ~ "`)" if model.label and model.label != model.name else model.display_label) | cell }} | {{ (model.description or "—") | cell }} | [Ver]({{ filenames[model.unique_id] }}) |
{% endfor %}

{% endfor %}
*Total de modelos: {{ project.models | length }}. Gerado a partir do schema dbt: {{ version }}.*
